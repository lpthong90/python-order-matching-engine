from tests.common import check


def test_add_order_1():
    inputs = [
        # List of order data
    ]
    outputs = [
        # Asks
        [
            # (price, volume)
        ],
        # Bids
        [
            # (price, volume)
        ],
        # Filled orders
        [
        ]
    ]
    check(inputs, outputs)

